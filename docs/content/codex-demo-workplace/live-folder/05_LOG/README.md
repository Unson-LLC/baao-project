# LOG

Codexの実行記録をrun_idごとに保存します。入力、実行モード、参照した本人の判断基準、重要判断、未確認事項、作成ファイル、外部送信の有無を残します。

`本人の判断基準あり`では、判断項目ごとに次を記録します。

| node_id | 適用した成果物 | 出力への影響 |
|---|---|---|
| 例：`pkg:persona-brain-first` | 顧客メール案 | 顧客の期限背景を先に受け止め、次に動ける確認事項を示した |

`会社ルールのみ`では「本人の判断基準は参照していない」と明記します。
